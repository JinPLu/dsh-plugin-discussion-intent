/** Browser entrypoint reserved for Discussion Rail and history contributions. */
export const name = 'discussion-intent-client'

/** No speculative UI registration precedes a versioned DSH read-only slot. */
export function apply(): void {}
