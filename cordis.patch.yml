# Public bundle-patch overlay (not a DSH source change): mount Discussion Mode.
# Subagent model starts empty; the adapter wrap asks from the live catalog.
- insert:
    - id: discussion-intent
      name: '@jinplu/dsh-plugin-discussion-intent'
      config:
        enabled: true
        defaultIntensity: 2
        directory: .dsh/discussions
